
#owl-demo.owl-carousel.owl-theme

	.item
		= image_tag("carousel_photos/blank.png")

	- @picked_images.each do |image|
		.item
			= image_tag("carousel_photos/#{image}.jpg", :class => "img-thumbnail")

	.item
		= image_tag("carousel_photos/blank.png")

.horizontal-ruler2
	%center.font-medium
		Začíname za :
.horizontal-ruler3
%center
	.countdown.styled